# Example environment variables for AI Document Summarization

# Database connection string (Prisma)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE"

# Server port (optional)
PORT=3000

# OpenRouter (LLM) configuration
OPENROUTER_API_KEY="your-openrouter-api-key"
# Optional: override the OpenRouter endpoint or model
OPENROUTER_URL="https://api.openrouter.ai/v1/chat/completions"
OPENROUTER_MODEL="gpt-4o-mini"

# S3 / Minio (optional) - when set, uploads will be attempted to S3
S3_ENDPOINT="https://minio.example.com"
S3_BUCKET="your-bucket"
S3_ACCESS_KEY_ID="your-access-key"
S3_SECRET_ACCESS_KEY="your-secret-key"
S3_REGION="us-east-1"
# Optional public URL (overrides constructed URL)
S3_PUBLIC_URL="https://cdn.example.com"

# Notes:
# - Uploaded files are stored in the local `uploads/` directory by default.
# - Copy this file to `.env` and fill in real values before running the app.
